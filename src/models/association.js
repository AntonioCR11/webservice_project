// IMPORT
const sequelize = require('../databases/connection');
const { Model, DataTypes } = require("sequelize");

// MODEL
// const exampleModel = require("./exampleModel")(sequelize, DataTypes);

module.exports = function() {
//   Buku.hasOne(KategoriBuku, {foreignKey: 'id_kategori'});
//   KategoriBuku.belongsTo(Buku, {foreignKey: 'id_kategori'});
}